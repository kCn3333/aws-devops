---
# Main playbook — applies all roles to all managed servers
# Usage: ansible-playbook playbooks/site.yaml

- name: Configure all managed servers
  hosts: all
  gather_facts: true

  roles:
    - role: common
    - role: nginx